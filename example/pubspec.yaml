name: share_example
description: Demonstrates how to use the share plugin.

dependencies:
  flutter:
    sdk: flutter
  share:
    path: ../

  camera: "^0.2.1"

  path_provider: "^0.4.1"

flutter:
  uses-material-design: true
